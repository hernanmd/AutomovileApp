export interface Automovile {
    id: number,
    plate: number;
    color: string;
} 

/* 
class Automovile {
  private readonly _id: number;
  private readonly _plate: number;
  private readonly _color: string;

  constructor(number: string, type: string, volume: string) {
    this._number = number;
    this._type = type;
    this._volume = volume;
  }

  get number(): string {
    return this._number;
  }

  get type(): string {
    return this._type;
  }

  get volume(): string {
    return this._volume;
  }

}

export default Sample;
*/